name: 项目健康检查

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  analyze:
    runs-on: ubuntu-latest
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
      
      - name: 显示信息
        run: echo "项目检查开始"
        
      - name: 查看文件
        run: ls -la
